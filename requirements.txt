# Web framework
fastapi==0.136.1
uvicorn[standard]==0.34.3

# Async HTTP client — used for LLM API calls (with timeout support)
httpx==0.28.1

# Testing
pytest==8.3.5
pytest-asyncio==1.3.0
respx==0.23.1          # httpx mock transport for deterministic test scenarios
anyio==4.9.0           # async test runner backend (used by pytest-asyncio)
httpx[cli]==0.28.1     # CLI extras for manual testing (optional)
